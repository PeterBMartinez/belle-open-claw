# 2026-04-02

- Peter reported the Pi SSH cross-access backlog item is complete.
- OpenClaw on pi-02 was restored from the oldest backup `~/.openclaw/openclaw.json.bak.4` (March 26), then restarted with `openclaw gateway --force`.
- OpenAI Codex model access was restored by recovering OAuth credentials from `openclaw.json.bak-2026-04-01-security`, adding auth profile `openai-codex:petermartinez225@gmail.com`, adding `gpt-5.4` to `~/.openclaw/agents/main/agent/models.json`, and setting it as the default with LM Studio fallback.
- Full local execution access was configured:
  - `~/.openclaw/openclaw.json`: `tools.exec.security=full`, `tools.exec.ask=off`, `tools.exec.host=gateway`
  - `agents.defaults.sandbox.mode=off`
  - `~/.openclaw/exec-approvals.json`: `agents.main.security=full`, `agents.main.ask=off`
- Revert path noted by Peter:
  - `cp ~/.openclaw/openclaw.json.bak-20260401-220655-pre-full-access ~/.openclaw/openclaw.json`
  - `openclaw gateway --force`
- Pi fleet SSH cross-access configured with mutual ed25519 keys and SSH config aliases across:
  - pi-01 (`100.84.93.86`) — RAG API, Qdrant, MinIO
  - pi-02 (`100.99.6.88`) — OpenClaw, DM Automations
  - pi-03 (`100.121.226.64`) — TBD
- Result: any Pi can now `ssh pi-01`, `ssh pi-02`, or `ssh pi-03` without prompts.
